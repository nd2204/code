#include <iostream>
using namespace std;
struct position {
  unsigned int row, col;
};
inline bool in_attack_range(position a, position b) {
  if (a.row + 3)
  return true;
}
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int N; cin >> N;
  position k1, k2;
  for (int i = 0;)
}
