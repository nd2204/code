let walker = [];
let ball = [];
let liquid;

function setup() {
  createCanvas(windowWidth, windowHeight);
  // background(22,23,21)
  background(0)
}

function draw() {
  background(22,23,21);
  rect(width/2, height/2, 50, 50);
  rotate(PI/2);
}
